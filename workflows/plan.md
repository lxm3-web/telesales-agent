# plan — 這週主推 XX，幫我排

輸入：主推商品名（對到 `data/方案頁/`）；沒講就先問。

1. 讀 `data/方案頁/00_索引.md` 確認商品存在、抓價格／贈品／折價券規則；不存在就停下來問。
2. 讀 `data/訂戶名單_300筆.csv`，依 `knowledge/02` 把 300 筆分成五群（對不上的標「待判」）。
3. 依 `knowledge/04` 決定哪幾群該打，逐群算分、排序，取前 30 通分給小美／阿凱／婷婷。
4. 寫產出到 `outbox/`（每檔最上面 `> 狀態：待確認`）：
   - `YYYY-MM-DD_主推XX_分群摘要.md`：五群人數、打哪幾群、不打哪幾群及理由
   - `YYYY-MM-DD_主推XX_今日撥打30通.csv`：順序、電銷、訂戶編號、姓名、電話、族群、分數、先傳LINE、理由
   - `YYYY-MM-DD_主推XX_話術卡_族群N.md`：要打的每一群一張（照 `03` 格式）
   - `YYYY-MM-DD_主推XX_暫緩名單.csv`：本週不打的，附理由
5. `log/campaign_log.md` 加一列
6. 依 `knowledge/05` 三行交件

## 不准做的
- 不准撥打、不准寄信、不准改名單
- 方案頁沒有的數字一律不寫
